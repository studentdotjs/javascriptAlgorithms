let maxRangeOf3rdEdge = function (edge1, edge2) {

    let thirdEdge = edge1 + edge2 - 1;
    console.log(`The maximum range of triangles third edge is ${thirdEdge}.`);

};

maxRangeOf3rdEdge(17, 37);
