let getIntegerSequence = function (startInt, endInt) {
    let sequencedIntegerArr = [];

    for (let i = startInt; i <= endInt; i++) {
        sequencedIntegerArr.push(i);
    }

    console.log(sequencedIntegerArr);

};

getIntegerSequence(17, 42);
