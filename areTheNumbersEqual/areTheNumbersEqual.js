let isNumEqual = function (num1, num2) {

    if (num1 === num2) {
        console.log(`${num1} and ${num2} are equal.`);
    } else {
        console.log(`${num1} and ${num2} are not equal.`);
    }

};

isNumEqual(17, 48);
