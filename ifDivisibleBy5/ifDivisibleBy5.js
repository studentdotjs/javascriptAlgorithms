function divisibleBy5(number) {
    if (number % 5 === 0) {
        console.log(`${number} is 'Fully divisible' by 5.`);
    } else {
        console.log(`${number} is 'Not divisible' by 5.`);
    }
}

divisibleBy5(101);
